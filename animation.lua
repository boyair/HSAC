
--vertical workspace switching
-- (works better with scrolling layout and side by side monitors)
hl.animation({
    leaf = "workspaces",
    enabled = true,
    speed = 8,
    bezier = "default",
    style = "slidevert"
})
